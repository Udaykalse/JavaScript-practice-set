// 2. Higher-Order Array Methods (map, filter, reduce)
function doubleNumbers(arr) {
    return arr.map(num => num * 2);
};

const num=[1,2,3,410,5];
const douNum=doubleNumbers(num);
console.log(douNum);
console.log(num)